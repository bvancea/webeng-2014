<li>
  <b><%= group.name %></b>
  <span style="padding-left: 25px"><%= group.description %></span>
  <span style="padding-left: 25px"><%= link_to "Edit", edit_group_path(group) %></span>
  <span style="padding-left: 25px"><%= link_to "Delete", group_path(group), method: :delete, data: { confirm: 'Are you certain you want to delete this?' } %></span>
  <span style="padding-left: 25px"><%= link_to "Show", group %></span>
</li>